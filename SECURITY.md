# 安全性

這支工具沒有伺服器、沒有帳號、不連網,所以這份文件的重點不是回報流程,
而是**講清楚它碰得到什麼、碰不到什麼**——你要自己判斷值不值得把有密碼的檔案交給它。

## 它做什麼

對於**你本來就知道開啟密碼**的 PDF,輸入一次密碼,輸出一份移除密碼的副本。
解密完全交由 [pypdf](https://github.com/py-pdf/pypdf) 把關,本工具沒有任何猜測、
暴力嘗試或繞過機制——密碼錯了就是解不開。

沒有輸入密碼時直接跳提示、不往下走,所以它也不會拿空密碼去剝除
「只設了權限密碼(擁有者密碼)」那類檔案的限制。

## 它碰得到什麼

| | |
|---|---|
| 網路 | 完全不連。沒有更新檢查、沒有遙測、沒有錯誤回報 |
| 你的密碼 | 只存在記憶體(tkinter 的輸入框)。不寫檔、不寫 log、不進剪貼簿、不隨錯誤訊息輸出 |
| 原始檔 | 只讀。解密後逐頁寫進**新檔**,不在原檔上動手腳 |
| 寫入位置 | 只有你在「另存」對話框選的那一個路徑。預設檔名是 `原檔名_無密碼.pdf`,不會蓋掉原檔 |
| 完成之後 | 用 `os.startfile()` 開啟輸出所在的資料夾,僅此而已 |

輸出的副本**沒有密碼保護**。它會躺在你選的資料夾裡,任何能讀那台電腦的人都打得開——
這就是這支工具的用途,但值得在解鎖公司或客戶的檔案之前想一下。

## 相依與散布

執行期只依賴 `pypdf` 與 `cryptography`(AES 解密後端),兩者都是 pip 上的主線套件。
Dependabot 已開啟。

[Releases](https://github.com/HSU-YU-MING/PDFUnlocker/releases) 提供的執行檔以 IV 程式碼簽章憑證
(`YU-MING HSU`,SSL.com 簽發)簽署並帶 RFC3161 時戳。**請只從 Releases 或 winget 取得**,
拿到後右鍵 → 內容 → 數位簽章確認發行者是 `YU-MING HSU`。
其他地方流傳的同名 exe 與本專案無關。

## 回報

只支援最新版,不回頭修舊版。

一般的錯誤直接開 [issue](https://github.com/HSU-YU-MING/PDFUnlocker/issues)——這支工具沒有
伺服器也沒有使用者資料,絕大多數問題公開講沒有風險,公開講反而讓下一個人搜得到。

真的需要私下談(例如簽章、散布管道相關),用 GitHub 的
[私密漏洞回報](https://github.com/HSU-YU-MING/PDFUnlocker/security/advisories/new)(已開啟)。

這是一個人維護的小工具,**不承諾回覆時間**。
